[package]
name = "styx-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "C bindings for the Styx configuration language parser"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
styx-tree.workspace = true

[build-dependencies]
cbindgen = "0.28"
