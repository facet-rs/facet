Clean up after a PR has been merged.

1. Checkout main: `git checkout main`
2. Pull latest: `git pull`
3. Optionally delete the merged branch locally: `git branch -d <branch>` (if we were on a feature branch)

This resets the working state to a clean main branch, ready for the next task.
