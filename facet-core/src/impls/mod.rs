// Definition for builtins;
mod builtin;

// Definition for `alloc::` types
mod alloc;

// Definiton for `core::` types
mod core;

// Definition for `std::` types (that aren't in `alloc` or `core)
mod std;

// Definition for various crates
mod crates;

// Definition for facet-core's own types
mod internal;
