{
  "name": "facet-perf-spa",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsgo --noEmit",
    "build": "tsgo --ignoreConfig app.ts --outDir . --target es2022 --module esnext",
    "validate": "tsx validate-run.ts"
  },
  "devDependencies": {
    "@typescript/native-preview": "*",
    "tsx": "^4.0.0"
  }
}
