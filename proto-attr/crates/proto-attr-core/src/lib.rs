//! Core types for the proto-attr grammar system.
//!
//! This crate provides the foundational types that extension crates
//! will use when defining attribute grammars.

/// Marker trait for attribute types generated by `define_attr_grammar!`.
///
/// This allows generic code to work with any attribute grammar.
pub trait AttrEnum: 'static + Sized {
    /// The name of this attribute grammar (e.g., "proto_ext")
    const NAME: &'static str;
}
