[package]
name = "facet-yaml"
version = "0.32.2"
edition = "2024"
rust-version = "1.89.0"
license = "MIT OR Apache-2.0"
repository = "https://github.com/facet-rs/facet-yaml"
description = "YAML serialization and deserialization for Facet types"
keywords = ["yaml", "serialization", "deserialization", "reflection", "facet"]
categories = ["encoding", "parsing", "data-structures"]

[package.metadata.docs.rs]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

[package.metadata."docs.rs"]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

[features]
ci = [] # CI feature
std = ["alloc", "facet-core/std", "facet-reflect/std"]
alloc = ["facet-core/alloc", "facet-reflect/alloc"]
default = ["std"]
axum = ["std", "dep:axum-core", "dep:http", "dep:http-body-util"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(feature, values(\"time\"))"] }

[dependencies]
saphyr-parser = "0.0.6"
facet = { path = "../facet", version = "0.32.2", default-features = false }
facet-core = { path = "../facet-core", version = "0.32.2", default-features = false }
facet-reflect = { path = "../facet-reflect", version = "0.32.2", default-features = false, features = ["miette"] }
facet-solver = { path = "../facet-solver", version = "0.32.2" }
miette = { version = "7", default-features = false }
log = "0.4.27"

# Axum integration (optional)
axum-core = { version = "0.5", default-features = false, optional = true }
http = { version = "1", default-features = false, optional = true }
http-body-util = { version = "0.1", default-features = false, optional = true }

[dev-dependencies]
facet = { path = "../facet", features = ["net", "nonzero"] }
eyre = "0.6.12"
time = { version = "0.3.41", default-features = false, features = ["macros", "parsing", "formatting"] }
facet-testhelpers = { path = "../facet-testhelpers" }
facet-showcase = { path = "../facet-showcase" }

[[example]]
name = "yaml_showcase"
path = "examples/yaml_showcase.rs"
