{
  "schema_version": 1,
  "id": "reject.duplicate-variant",
  "outcome": "reject",
  "description": "Enum variant names must be unique within an enum.",
  "sources": [
    {
      "path": "reject/duplicate-variant.vix",
      "module": "crate"
    }
  ],
  "assertions": [
    {
      "query": "diagnostics",
      "subject": "DuplicateVariant",
      "expect": "enum=Mode variant=Fast first_span=16..20",
      "span_start": 26,
      "span_end": 30
    }
  ]
}
