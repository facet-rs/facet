// Rung 044 — Set<T>: membership without Map<T, Bool> workarounds.
#[test]
fn sets() -> Stream<Check> {
    let s: Set<Int> = %[3, 1, 3];
    yield expect_eq(s.len(), 2);
    yield expect(s.contains(3));
    yield expect(!s.contains(2));
    yield expect_eq(s.keys().sorted(), [1, 3]);
}
