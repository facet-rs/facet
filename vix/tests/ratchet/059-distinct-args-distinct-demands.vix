// Rung 059 — different arguments are different demands (negative control).
fn costly(n: Int) -> Int { n * 1000 }

test distinct_args_distinct_demands {
    expect_eq(costly(1) + costly(2), 3000);
} expecting {
    demanded_times costly 2;
}
