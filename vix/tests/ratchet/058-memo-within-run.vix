// Rung 058 — one demand key, one computation: sharing within a run.
fn costly(n: Int) -> Int { n * 1000 }

test memo_within_run {
    let a = costly(7);
    let b = costly(7);
    expect_eq(a, b);
    expect_eq(a + b, 14000);
} expecting {
    demanded_once costly;
}
