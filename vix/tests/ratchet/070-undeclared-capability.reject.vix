//! reject: undeclared capability
//! at: 6

// Rung 070 — using a tool without declaring its capability is loud. (effects)
#[test]
fn undeclared_capability() -> Stream<Check> {
    let out = exec! { cc -o hello hello.c };
    yield expect_eq(out.status, 0);
}
