// Rung 026 — array literals, indexing, len. Arrays are structs. (collections)
test arrays {
    let xs = [10, 20, 30];
    expect_eq(xs[0], 10);
    expect_eq(xs[2], 30);
    expect_eq(xs.len(), 3);
}
