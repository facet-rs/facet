// Rung 019 — tuple destructuring in let.
fn min_max(a: Int, b: Int) -> (Int, Int) {
    if a < b { (a, b) } else { (b, a) }
}

test let_destructuring {
    let (lo, hi) = min_max(9, 4);
    expect_eq(lo, 4);
    expect_eq(hi, 9);
}
