// Second phase for rung 104: same leaf, now reached through a wrapper.
fn leaf(n: Int) -> Int { n * 7 }
fn pipeline(n: Int) -> Int { leaf(n) }

test wrapper_refactor_warm {
    expect_eq(pipeline(6), 42);
}
