{
  "name": "@bearcove/phon-typescript",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter './typescript/packages/*' run build",
    "check": "pnpm -r run check",
    "sync-ts-package-versions": "node scripts/sync-ts-package-versions.mjs",
    "test": "pnpm -r run test",
    "lint": "oxlint typescript"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "oxlint": "^1.71.0",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
