[package]
name = "phon-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The IR both phon backends run, the descriptor model, and thunk bindings — the shared vocabulary of execution."

[dependencies]
phon-schema.workspace = true
