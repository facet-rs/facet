<INSTRUCTIONS>
You are working in `java/`, which is the new Java implementation area.

Rules:
- Prefer adding new code here.
- There is no legacy Java implementation yet.
- The compliance suite lives under `spec/` and runs subjects via `SUBJECT_CMD`.
</INSTRUCTIONS>

