# Swift subject (stub)

This is a placeholder for the Swift subject binary used by `spec/spec-tests`.

CI will eventually run:
- `SUBJECT_CMD='<swift subject command>' cargo nextest run -p spec-tests`

