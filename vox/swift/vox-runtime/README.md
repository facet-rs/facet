# Vox Runtime (Swift)

Swift bindings for the Vox protocol.

This crate provides:

- `VoxRuntime`: protocol connection and lane helpers
- `Wire` protocol helpers and codecs

See the root `README.md` and the generated test fixtures in `Tests/` for usage examples.
