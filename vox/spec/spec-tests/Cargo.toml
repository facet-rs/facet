[package]
name = "spec-tests"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Compliance tests for the canonical Rapace spec in docs/content/"

[dependencies]
rapace-wire = { path = "../../rust/rapace-wire" }

[dev-dependencies]
cobs.workspace = true
facet-postcard.workspace = true
tokio = { workspace = true, features = ["net", "io-util", "time", "process"] }
