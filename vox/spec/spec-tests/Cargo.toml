[package]
name = "spec-tests"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Compliance tests for the canonical roam spec in docs/content/"

[dependencies]
roam-wire = { path = "../../rust/roam-wire" }
cobs.workspace = true
facet-postcard.workspace = true
tokio = { workspace = true, features = ["net", "io-util", "time", "process"] }
spec-proto = { path = "../spec-proto" }

[dev-dependencies]
facet.workspace = true
roam-hash = { path = "../../rust/roam-hash" }
roam-reflect = { path = "../../rust/roam-reflect" }
roam-schema = { path = "../../rust/roam-schema" }
roam-service-macros = { path = "../../rust/roam-macros" }
