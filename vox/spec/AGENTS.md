<INSTRUCTIONS>
You are working in `spec/`, which is the **current** compliance suite targeting the canonical spec in `docs/content/`.

Rules:
- Prefer adding new tests and harness code in `spec/`.
- Treat `spec-legacy/` as reference-only unless explicitly asked to modify it.
</INSTRUCTIONS>

