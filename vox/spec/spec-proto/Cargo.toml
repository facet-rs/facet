[package]
name = "spec-proto"
version = "3.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Service definitions used by the canonical roam compliance suite"

[package.metadata]

[package.metadata."docs.rs"]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

[dependencies]
facet.workspace = true
roam = { path = "../../rust/roam" }
