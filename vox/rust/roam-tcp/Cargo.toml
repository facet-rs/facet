[package]
name = "roam-tcp"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "TCP transport layer for roam RPC (COBS framing, connection management, streaming)"

[package.metadata]

[package.metadata."docs.rs"]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

[dependencies]
cobs.workspace = true
tokio = { workspace = true, features = ["net", "io-util", "rt", "time", "sync"] }
roam-wire = { path = "../roam-wire", version = "0.5.0" }
roam-session = { path = "../roam-session", version = "0.5.0" }
facet-postcard.workspace = true
