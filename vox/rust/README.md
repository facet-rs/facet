# Rust implementation

This directory contains the **new** Rust implementation that targets the canonical spec in `docs/content/`.

Legacy code lives in `rust-legacy/` and should be treated as reference-only unless you are
explicitly asked to modify it.
