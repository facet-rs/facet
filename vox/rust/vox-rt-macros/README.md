# vox-rt-macros

No-op runtime facade macros for Vox.

This crate is an implementation detail of `vox-rt` and is published so the
workspace can expose a stable runtime facade across native and wasm targets.
