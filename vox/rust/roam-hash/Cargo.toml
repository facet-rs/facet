[package]
name = "roam-hash"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Hashing and method identity for roam (rust-spec)"

[dependencies]
blake3.workspace = true
roam-schema = { path = "../roam-schema" }
