[package]
name = "codegen-test-proto"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Test proto crate for validating build.rs codegen approach"

[dependencies]
roam = { path = "../roam" }
