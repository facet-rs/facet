<INSTRUCTIONS>
This directory contains the **legacy** compliance suite (historical reference only).

Rules:
- Avoid editing files under `spec-legacy/` unless explicitly requested.
- New compliance tests belong under `spec/` and target the canonical spec in `docs/content/`.
</INSTRUCTIONS>

