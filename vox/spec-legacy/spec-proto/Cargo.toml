[package]
name = "rapace-spec-proto"
version = "0.1.0"
edition = "2024"
description = "Shared service definitions for rapace spec tests"
license = "MIT OR Apache-2.0"
publish = false

[package.metadata]

[package.metadata."docs.rs"]
rustdoc-args = ["--html-in-header", "arborium-header.html"]

# TODO: These will be used when we add actual service definitions
[package.metadata.cargo-shear]
ignored = ["rapace", "facet"]

[dependencies]
rapace.workspace = true
facet.workspace = true
