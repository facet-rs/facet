<INSTRUCTIONS>
Shared service definitions for spec tests.

Note:
- This currently depends on `rapace.workspace` (legacy). The new suite in `spec/` will likely need a parallel proto crate once the new macro/codegen story lands.
</INSTRUCTIONS>
