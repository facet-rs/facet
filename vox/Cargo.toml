[workspace]
resolver = "2"
members = [
    # New Rust implementation
    "rust/rapace-wire",
    "rust/rapace-codec",
    "rust/rapace-session",
    "rust/rapace-hash",
    "rust/rapace-schema",
    "rust/rapace-shm",
    "rust/rapace-codegen",
    "rust/rapace-macros",
    "rust/subject-rust",
    # New compliance suite
    "spec/spec-tests",
    "demos/dashboard",
    "demos/browser-tests-proto",
    "demos/browser-tests-server",
    "demos/browser-tests-client",
    "demos/demo-helper-common",
    "demos/http-over-rapace",
    "demos/diagnostics-over-rapace",
    "demos/template-engine",
    "demos/tracing-over-rapace",
    "demos/http-tunnel",
    "test-external-service",
    "xtask",
    # Legacy compliance suite (kept for reference)
    "spec-legacy/spec-peer",
    "spec-legacy/spec-peer-macros",
    "spec-legacy/spec-proto",
    "spec-legacy/spec-subject",
    "spec-legacy/spec-tests",
]
exclude = ["fuzz"]

default-members = [
    "rust/rapace-wire",
    "rust/rapace-codec",
    "rust/rapace-session",
    "rust/rapace-hash",
    "rust/rapace-schema",
    "rust/rapace-shm",
    "rust/rapace-codegen",
    "rust/rapace-macros",
]

[workspace.package]
version = "0.5.0"
edition = "2024"
rust-version = "1.91"
license = "MIT OR Apache-2.0"
repository = "https://github.com/bearcove/rapace"

[workspace.dependencies]
# Internal crates
rapace = { path = "rust-legacy/rapace", version = "0.5.0" }
rapace-core = { path = "rust-legacy/rapace-core", version = "0.5.0" }
rapace-protocol = { path = "rust-legacy/rapace-protocol", version = "0.5.0" }
rapace-http = { path = "rust-legacy/rapace-http", version = "0.5.0" }
rapace-macros = { path = "rust-legacy/rapace-macros", version = "0.5.0" }
rapace-registry = { path = "rust-legacy/rapace-registry", version = "0.5.0" }
rapace-tracing = { path = "rust-legacy/rapace-tracing", version = "0.5.0" }
shm-primitives = { path = "rust-legacy/shm-primitives", version = "0.5.0" }

# External dependencies
libc = "0.2"
bitflags = "2"
bytes = "1"
blake3 = "1.8"
cobs = "0.5.0"
tokio = { version = "1", features = ["sync", "rt", "time"] }
tokio-stream = "0.1"
# futures crates - minimal set without proc-macro overhead
futures-core = "0.3"
futures-util = { version = "0.3", default-features = false, features = ["std", "async-await", "sink"] }
parking_lot = "0.12"

# facet ecosystem
facet = { git = "https://github.com/facet-rs/facet", branch = "main", features = ["camino"] }
facet-core = { git = "https://github.com/facet-rs/facet", branch = "main" }
facet-json = { git = "https://github.com/facet-rs/facet", branch = "main" }
facet-postcard = { git = "https://github.com/facet-rs/facet", branch = "main", features = ["jit", "camino"] }
facet-cargo-toml = { git = "https://github.com/bearcove/facet-cargo-toml", branch = "main" }

# WebSocket
tokio-tungstenite = "0.28"

# Web framework (no tokio feature = no tokio-macros = no syn)
axum = { version = "0.8", default-features = false, features = [
    "form", "http1", "json", "matched-path", "original-uri",
    "query", "tower-log", "tracing", "ws"
] }
rapace-axum-serve = { path = "rust-legacy/rapace-axum-serve" }

# Tracing
tracing = { version = "0.1", default-features = false, features = ["std"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Allocator API
allocator-api2 = "0.4"
static_assertions = "1.1.0"

# Buffer pool
object-pool = "0.6"

# FD passing utilities
async-send-fd = { version = "1.2", features = ["tokio"] }

# Testing
tokio-test-lite = "0.2"

# Wasm
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = { version = "0.3", features = ["console", "WebSocket", "MessageEvent", "BinaryType", "ErrorEvent", "CloseEvent"] }
gloo-timers = { version = "0.3", features = ["futures"] }
web-time = "1"



[profile.release]
debug = 1
