# snark

Snark is a Tree-sitter-compatible grammar package and parser runtime scaffold.

The first target is importing real Tree-sitter language packages, starting with
CSS, while preserving the architecture needed for external scanners, queries,
incremental parsing, diagnostics, and injected languages.

