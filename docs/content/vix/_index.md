+++
title = "vix"
+++

The vix language and its machine: specifications with requirement coverage.
Rules defined here are traced to implementations via `r[impl ...]` /
`r[verify ...]` references (see `ddc coverage`).
